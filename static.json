{
    "root": "build/",
    "routes": {
        "/**": "index.html"
      },
    "proxies": {
        "/api/": {
            "origin": "${API_URL}"
        },
        "/uploads/": {
            "origin": "${UPLOAD_URL}"
        }
    }
}